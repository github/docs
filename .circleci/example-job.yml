jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - codecov/upload
workflows:
  example-workflow:
    jobs:
      - example-job