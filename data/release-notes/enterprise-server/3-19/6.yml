date: '2026-05-07'
sections:
  features:
    - |
      Site administrators can use the Enterprise Live Migrations (ELM) service to migrate repositories from GitHub Enterprise Server to a data-resident enterprise on GHE.com. Live migrations carry less downtime for developers than other migration tools, because the source repository is only locked during the cutover phase. This feature is in public preview and subject to change. See [AUTOTITLE](/migrations/elm/about-live-migrations).
    - |
      To prevent Redis replication failures in clusters with high write volumes, administrators can configure Redis replication backlog size and client output buffer limits via `ghe-config`. Previously, these settings used fixed defaults that could cause replica disconnections when write throughput exceeded buffer limits, resulting in continuous full-resync loops. Changes take effect after running a configuration run.
    - |
      The capability to configure multiple data disks to host MySQL and repository data is now generally available. This applies to standalone, and high-availability topologies, and it is generally available in the latest patch versions of 3.17, 3.18, 3.19, and 3.20. See [AUTOTITLE](/admin/monitoring-and-managing-your-instance/multiple-data-disks/configuring-multiple-data-disks).
  security_fixes:
    - |
      **HIGH**: An attacker could exploit a server-side request forgery (SSRF) vulnerability in the notebook viewer to access internal services by exploiting URL parser confusion between the validation layer and the HTTP request library. This attack required network access to the GHES instance with private mode disabled. The vulnerability was addressed by aligning URL parsing between the validation and request layers and adding explicit validation of URL scheme, port, and userinfo components. GitHub has requested CVE ID [CVE-2026-8034](https://www.cve.org/cverecord?id=CVE-2026-8034) for this vulnerability, which was reported via the [GitHub Bug Bounty program](https://bounty.github.com).
    - |
      **MEDIUM**: An attacker could steal Management Console credentials by injecting malicious HTML into the login page via a crafted URL. The `redirect_to` query parameter was not properly sanitized, allowing an attacker to embed a form element that captured credentials submitted on the page. To mitigate this issue, GitHub replaced the template engine with one that properly enforces HTML auto-escaping. GitHub has requested CVE ID [CVE-2026-8106](https://www.cve.org/cverecord?id=CVE-2026-8106) for this vulnerability, which was reported via the [GitHub Bug Bounty program](https://bounty.github.com).
    - |
      **MEDIUM**: An attacker could cause a denial of service on the instance by sending crafted requests with deeply nested JSON payloads to an unauthenticated API endpoint. To mitigate this issue, GitHub has added maximum size and depth validation to the stats endpoint. GitHub has requested CVE ID [CVE-2026-7541](https://www.cve.org/cverecord?id=CVE-2026-7541) for this vulnerability, which was reported via the [GitHub Bug Bounty](https://bounty.github.com/) program.
    - |
      **MEDIUM**: An attacker could create a local user account on a GitHub Enterprise Server instance configured with an external authentication provider, bypassing the identity provider entirely. The signup endpoint did not properly enforce the external authentication restriction, allowing account creation and session establishment without identity provider validation. The created account was limited to the default base permissions configured on the instance. GitHub has requested CVE ID [CVE-2026-6736](https://www.cve.org/cverecord?id=CVE-2026-6736) for this vulnerability.
    - |
      **LOW**: An attacker could discover the existence and URLs of private repositories by making unauthorized REST API requests to public repository issues with parent issues in private repositories. The REST API unconditionally included the `parent_issue_url` field in issue responses, exposing private repository names, owners, and issue numbers to unauthenticated users or users without access to the parent repository. This vulnerability was reported via the [GitHub Bug Bounty](https://bounty.github.com/) program.
  bugs:
    - |
      Users experienced failures with token-based authentication after the authnd service started but could not bind its HTTP port, leaving the service running but unable to serve requests.
    - |
      Administrators who cancelled `ghe-live-migrations` (Ctrl+C or SIGTERM) during a live upgrade found that the CLI exited but the live-upgrade container continued running in the background. The container now stops gracefully when the command is interrupted.
    - |
      Administrators found that SSH settings for agents on cloud platforms (such as Googles guest agent) did not persist after upgrading GitHub Enterprise Server, requiring reconfiguration after each upgrade. These settings are now carried over between upgrades.
    - |
      On instances with high push activity, commits did not appear in pull requests for minutes to hours, webhooks were delayed, and pull request state appeared stale. The stream processor service ran as a single process per node and could not scale horizontally to handle message volume.
    - |
      When initially configuring GitHub Actions with OIDC authentication, the configuration failed with an error indicating that OIDC was not enabled.
    - |
      On instances configured for high availability, GitHub Actions settings could not be changed through the Management Console.
    - |
      On instances using Enterprise teams, background jobs for enterprise team organization reconciliation accumulated in the queue and repeatedly failed when organization teams or team mappings were deleted while reconciliation jobs were running. This caused elevated latency for other background jobs.
    - |
      On an instance with SAML authentication and GitHub Pages configured, users were redirected to the root of the instance instead of their intended destination after completing SAML authentication.
    - |
      When delegated dismissal for secret scanning was enabled, users could submit dismissal requests for already-closed secret scanning alerts. These requests could not be reviewed or resolved via the UI, leaving alerts in a "request pending" state.
    - |
      The metrics page for secret scanning and push protection on security overview failed to load with an encoding error when pagination cursors contained binary data that could not be converted from UTF-8 to ASCII.
    - |
      On instances where persistent commit signature verification was disabled, commit quarantine processing jobs were still queued and backed up background job queues.
    - |
      On the secret scanning alert page for a generic secret, a feedback banner linked to a survey that no longer exists.
    - |
      Site administrators saw an “Automatically request Copilot code review” branch protection rule on GitHub Enterprise Server, even though the setting was not intended to be available on GHES.
    - |
      When exporting bot-initiated issue events for migrations, bot users were not resolved to a valid URL.
    - |
      When delegated alert dismissal was enabled for secret scanning, users without reviewer permissions saw a "Reopen alert" button on closed secret scanning alerts. Clicking the button returned a "Not Found" error.
  changes:
    - |
      The `ghe-cluster-hotpatch` command now supports the `--parallel` flag. This flag executes the rollover stage in parallel across all nodes in a cluster, saving a significant amount of time during upgrades of large clusters.
    - |
      To support phased upgrades for future versions of GitHub Enterprise Server, site administrators can now use the `--phase` flag with the `ghe-upgrade` command.
    - |
      To prevent collectd memory exhaustion and OOM kills caused by write queue growth, administrators can configure collectd write queue limits and memory caps via `ghe-config`. Previously, tuning these settings required manual edits to collectd.conf and systemd configuration files. Changes take effect after running a configuration apply.
  known_issues:
    - |
      During an upgrade of GitHub Enterprise Server, custom firewall rules are removed. If you use custom firewall rules, you must reapply them after upgrading.
    - |
      During the validation phase of a configuration run, a `No such object` error may occur for the Notebook and Viewscreen services. This error can be ignored as the services should still correctly start.
    - |
      If the root site administrator is locked out of the Management Console after failed login attempts, the account does not unlock automatically after the defined lockout time. Someone with administrative SSH access to the instance must unlock the account using the administrative shell. For more information, see [Troubleshooting access to the Management Console](/admin/administering-your-instance/administering-your-instance-from-the-web-ui/troubleshooting-access-to-the-management-console#unlocking-the-root-site-administrator-account).
    - |
      {% data reusables.release-notes.large-adoc-files-issue %}
    - |
      Admin stats REST API endpoints may timeout on appliances with many users or repositories. Retrying the request until data is returned is advised.
    - |
      When following the steps for [Replacing the primary MySQL node](/admin/monitoring-managing-and-updating-your-instance/configuring-clustering/replacing-a-cluster-node#replacing-the-primary-mysql-node), step 14 (running `ghe-cluster-config-apply`) might fail with errors. If this occurs, re-running `ghe-cluster-config-apply` is expected to succeed.
    - |
      Running a config apply as part of the steps for [Replacing a node in an emergency](/admin/monitoring-managing-and-updating-your-instance/configuring-clustering/replacing-a-cluster-node#replacing-a-node-in-an-emergency) may fail with errors if the node being replaced is still reachable. If this occurs, shutdown the node and repeat the steps.
    - |
      {% data reusables.release-notes.2024-06-possible-frontend-5-minute-outage-during-hotpatch-upgrade %}
    - |
      When restoring data originally backed up from a 3.13 or greater appliance version, the Elasticsearch indices need to be reindexed before some of the data will show up.  This happens via a nightly scheduled job.  It can also be forced by running `/usr/local/share/enterprise/ghe-es-search-repair`.
    - |
      When enabling automatic update checks for the first time in the Management Console, the status is not dynamically reflected until the "Updates" page is reloaded.
    - |
      When restoring from a backup snapshot, a large number of `mapper_parsing_exception` errors may be displayed.
    - |
      When initializing a new GHES cluster, nodes with the `consul-server` role should be added to the cluster before adding additional nodes. Adding all nodes simultaneously creates a race condition between nomad server registration and nomad client registration.
    - |
      Admins setting up cluster high availability (HA) may encounter a spokes error when running `ghe-cluster-repl-status` if a new organization and repositories are created before using the `ghe-cluster-repl-bootstrap` command. To avoid this issue, complete the cluster HA setup with `ghe-cluster-repl-bootstrap` before creating new organizations and repositories.
    - |
      In a cluster, the host running restore requires access the storage nodes via their private IPs.
    - |
      On an instance hosted on Azure, commenting on an issue via email meant the comment was not added to the issue.
    - |
      After a restore, existing outside collaborators cannot be added to repositories in a new organization. This issue can be resolved by running `/usr/local/share/enterprise/ghe-es-search-repair` on the appliance.
    - |
      After a geo-replica is promoted to be a primary by running `ghe-repl-promote`, the actions workflow of a repository does not have any suggested workflows.
    - |
      When publishing npm packages in a workflow after restoring from a backup to GitHub Enterprise Server 3.13.5.gm4 or 3.14.2.gm3, you may encounter a `401 Unauthorized` error from the GitHub Packages service. This can happen if the restore is from an N-1 or N-2 version and the workflow targets the npm endpoint on the backup instance. To avoid this issue, ensure the access token is valid and includes the correct scopes for publishing to GitHub Packages.
    - |
      The setting to define private registries at the organization level for code scanning is only available if dependabot is also enabled for the instance.
    - |
      Upgrading or hotpatching to 3.19.1 may fail on nodes that have been continuously upgraded from versions older than 2021 (i.e. 2.17). If this issue occurs, you will see log entries prefixed with `invalid secret` in ghe-config.log. If you are running nodes from these older versions, it is recommended not to upgrade to 3.19.1.
    - |
      In patch 3.19.1, we identified an issue in the Management Console where the Backups (Preview) and Updates tabs may fail to open and instead return an Internal Server Error. We recommend using the command line interface (CLI) for backups and updates until an updated patch is released.
    - |
      When applying an enterprise security configuration to all repositories (for example, enabling Secret Scanning or Code Scanning across all repositories), the system immediately enqueues enablement jobs for every organization in the enterprise simultaneously. For enterprises with a large number of repositories, this can result in significant system load and potential performance degradation. If you manage a large enterprise with many organizations and repositories, we recommend applying security configurations at the organization level rather than at the enterprise level in the UI. This allows you to enable security features incrementally and monitor system performance as you roll out changes.
