---
gist_homepage: >-
  {% if currentVersion == "free-pro-team@latest" %}[gist 主页](https://gist.github.com/){% elsif currentVersion == "github-ae@latest" %}gist 主页 `http(s)://gist.[hostname)`，{% else %}gist 主页 `http(s)://[hostname]/gist` 或 `http(s)://gist.[hostname]` 如果启用了子域{% endif %}
gist_search_url: >-
  {% if currentVersion == "free-pro-team@latest" %}[Gist 搜索](https://gist.github.com/search){% elsif currentVersion == "github-ae@latest" %}Gist 搜索 `http(s)://gist.[hostname]/search`，{% else %}Gist 搜索 `http(s)://[hostname]/gist/search` 或 `http(s)://gist.[hostname]/search`（如果启用了子域）{% endif %}
discover_url: >-
  {% if currentVersion == "free-pro-team@latest" %}[Discover](https://gist.github.com/discover){% elsif currentVersion == "github-ae@latest" %}Discover, `http(s)://gist.[hostname]/discover`，{% else %}Discover `http(s)://[hostname]/gist/discover` 或 `http(s)://gist.[hostname]/discover`（如果启用了子域）{% endif %}
