使用 `jobs.<job_id>.defaults` 创建将应用于作业中所有步骤的默认设置 `map`。 您也可以设置整个工作流程的默认设置。 更多信息请参阅 [`defaults`](/actions/using-workflows/workflow-syntax-for-github-actions#defaults)。

{% data reusables.actions.defaults-override %}
