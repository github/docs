*client.js* 脚本寻找 `POSTGRES_HOST` 和 `POSTGRES_PORT` 环境变量来创建客户端。 工作流程将这两个环境变量设置为“连接到 PostgreSQL”步骤的一部分，使它们可用于 *client.js* 脚本。 关于脚本的更多信息，请参阅“[测试 PostgreSQL 服务容器](#testing-the-postgresql-service-container)”。
