在 `if` 条件下使用表达式时，可以省略表达式语法 ({% raw %}`${{ }}`{% endraw %})，因为 {% data variables.product.prodname_dotcom %} 会自动将 `if` 条件作为表达式求值，除非表达式包含任何运算符。 如果表达式包含任何运算符，则表达式必须包含在 {% raw %}`${{ }}`{% endraw %} 内，以明确标记它进行计算。
