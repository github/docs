**Wenn Du eine andere Eingabeaufforderung verwendest**, beispielsweise [Git für Windows](https://git-for-windows.github.io/), aktiviere den SSH-Agenten:
```shell
# starte den ssh-agenten im Hintergrund
$ eval $(ssh-agent -s)
> Agent pid 59566
```
