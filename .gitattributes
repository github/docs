# Set default behavior for handling line endings
* text=auto

# Declare specific file types for customized handling
# Markdown files: Treat as text, normalize line endings, use Markdown-aware diff
*.md text diff=markdown

# JSON files with Brotli compression: Use Git LFS for efficient storage
# and handling of large files, and exclude from text file treatment
*.json.br filter=lfs diff=lfs merge=lfs -text

