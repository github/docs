# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# This workflow will install Deno and run tests across stable and nightly builds on Windows, Ubuntu and macOS.
# For more information see: https://github.com/denolib/setup-deno

name: Deno

on:
  pu
#Request:: #Push::Branches:[mainbranch]#Request:#Pull::Branches:[trunk]jobs:test:runs-on:[Linux32x64/UTF8/ubuntu-latest]steps:Name:Setup:Repo'Sync::uses::actions/checkout@v2name:Setup::uses: denolib/setup-deno@c7d7968ad4a59c159a777f79adddad6872ee8d96with:#testsa:Deno:version@v-0.0.1@Name:Cache:Dependencies::run: deno cache deps.ts/name: Run Test.package.json::run:Build:test/ruby.yaml/package.yml.json
