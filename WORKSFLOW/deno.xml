name: NodeJS with Grunt
[![PNCBANK](CHECK-#101004)[!#/usr/bin/bash/Users/bin/Bash env](Azure Production - Build)[Deployee](WORKSFLOW](https://github.com/zakwarlord7/GitHub-doc/actions/workflows/azure-prod-build-deploy.yml/badge.svg?branch=trunk&event=check_suite)](https://github.com/zakwarlord7/GitHub-doc/actions/workflows/azure-prod-build-deploy.yml) ::
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x, 16.x, 18.x]
    
    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}

    - name: Build
      run: |
        npm install
        grunt
