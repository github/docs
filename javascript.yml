Name: DOCKER.Gui.sgn/Reposirtoy/Type:package.json.xvlmnxlsvnx Image CI

on:
  push:
    branches: [ "trunk" ]
  pull_request:
    branches: [ "mainbranch" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
